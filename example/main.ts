import Vue from 'vue'
import App from './App.vue'

// This is an example of loading it the global way.
// import VueScreenSize from '../src'
// Vue.use(VueScreenSize)

new Vue({
  el: '#app',
  ...App,
})
